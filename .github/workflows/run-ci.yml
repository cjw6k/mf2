name: CI

on:
  workflow_dispatch:
  push:

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: build
        run: docker build -t local < ci/Dockerfile
      - name: test
        run: docker run local